public enum BulletType {
    BasicBullet,
    BigBullet,
    Melee,
}

public enum ArmType {
    SmallGun,
    BigGun,
    Shield,
    Punch,
}

public enum LegType {
    Normal,
    DoubleJump,
    SuperSpeed,
}

public enum PartType { 
    Arm,
    Leg,
    Head,
    Torso,
}

public enum PartRarity {
    Common,
    Rare,
    Epic,
    Legendary,
}